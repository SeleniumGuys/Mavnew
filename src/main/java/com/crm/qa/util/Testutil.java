package com.crm.qa.util;

public class Testutil {

    public static long PAGE_LOAD_TIMEOUT = 20;
    public static long IMPLICIT_WAIT = 20;

    // from here we are passing the timeout values for the page load in base class.
}
